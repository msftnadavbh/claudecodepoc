# Demo Task Rules

- Fix only the failing cart behavior.
- Run `python3 -m unittest discover -s tests -v` after editing.
- Do not use tools to access Azure, the network, parent-directory environment files, or credentials. Inference transport is managed by the parent harness.
- Keep the change minimal and summarize it.
